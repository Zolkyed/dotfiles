---
- name: Full system setup
  hosts: all
  become: true
  gather_facts: false

  pre_tasks:
    - name: Gather facts
      ansible.builtin.setup:

    - name: Load OS-specific variables
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/../vars/{{ distro | default(ansible_os_family) }}.yml"

    - name: Load encrypted secrets
      community.sops.load_vars:
        file: "{{ playbook_dir }}/../../secrets/vault.yml"
      become: false
      ignore_errors: true

  roles:
    - role: home/packages
    - role: home/hayase
    - role: home/user
    - role: system/fonts
    - role: system/flatpak
    - role: system/docker
    - role: system/nvidia
      when: machine_type == "desktop"
    - role: system/vm
    - role: home/dotfiles
    - role: home/ssh_keys
    - role: home/dev
    - role: home/bin
    - role: system/networking
    - role: system/ssh
    - role: system/bluetooth
    - role: system/bootloader
    - role: system/display_manager
    - role: desktop/kde
    - role: desktop/konsave
    - role: home/gaming
      when: machine_type == "desktop"

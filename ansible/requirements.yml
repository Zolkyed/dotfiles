---
collections:
  - name: community.general   # flatpak and kdeconfig helpers
  - name: ansible.posix       # authorized_key, mount, sysctl
  - name: community.sops      # load_vars from SOPS-encrypted vault
  - name: community.docker    # manage Docker containers/networks/volumes post-install

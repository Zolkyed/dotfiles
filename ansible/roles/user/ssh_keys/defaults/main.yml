---
ssh_keys_list:
  - name: "{{ inventory_hostname }}"
    private_key: "{{ ansible_facts['vault_ssh_private_key'] | default('') }}"
    public_key: "{{ ansible_facts['vault_ssh_public_key'] | default('') }}"

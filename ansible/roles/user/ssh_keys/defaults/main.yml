---
# List of SSH keys to deploy
# ssh_keys:
#   - name: id_ed25519
#     private_key: "{{ lookup('file', playbook_dir + '/../../secrets/ssh/id_ed25519') }}"
#     public_key: "{{ lookup('file', playbook_dir + '/../../secrets/ssh/id_ed25519.pub') }}"
ssh_keys: []
ssh_authorized_keys: []

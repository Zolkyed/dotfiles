# systemd-resolved configuration managed by Ansible
[Resolve]
DNS={{ networking_dns_servers | join(' ') }}
FallbackDNS={{ networking_dns_fallback_servers | join(' ') }}
DNSSEC={{ networking_dns_dnssec }}
DNSOverTLS={{ networking_dns_over_tls }}
Cache=yes

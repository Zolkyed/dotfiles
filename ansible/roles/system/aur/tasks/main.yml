---
- name: Aur | Install build prerequisites
  community.general.pacman:
    name:
      - base-devel
      - git
    state: present

- name: Aur | Install paru
  kewlfft.aur.aur:
    name: paru
    use: makepkg
    state: present
  become: true
  become_user: "{{ user_name }}"

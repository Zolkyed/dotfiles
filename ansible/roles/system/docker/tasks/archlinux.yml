---
- name: Docker | Install packages
  community.general.pacman:
    name: "{{ pacman_packages.docker_engine + pacman_packages.docker_plugins }}"
    state: present
  notify: Docker | Restart docker

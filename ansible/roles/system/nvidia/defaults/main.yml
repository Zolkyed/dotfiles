---
# Use 'auto' to let ubuntu-drivers pick the recommended driver
# Or pin to a specific version e.g. 'nvidia-driver-580'
nvidia_driver_package: auto

nvidia_packages: []

# Performance mode: 1=max perf, 0=auto
nvidia_performance_level: 1

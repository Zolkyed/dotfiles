---
- name: Check if Hayase is already installed
  ansible.builtin.command:
    cmd: dpkg-query -W -f='${Status}' hayase
  register: home_hayase_pkg_check
  changed_when: false
  failed_when: false
  when: ansible_facts["os_family"] == "Debian"

- name: Check if Hayase AppImage exists
  ansible.builtin.stat:
    path: "{{ hayase_appimage_dest }}"
  register: home_hayase_appimage_stat
  when: ansible_facts["os_family"] == "Archlinux"

- name: Get Hayase latest release info
  ansible.builtin.uri:
    url: "{{ hayase_api_url }}"
    return_content: true
  register: home_hayase_release
  when: >
    (ansible_facts["os_family"] == "Debian" and
     home_hayase_pkg_check.stdout | default('') != 'install ok installed')
    or
    (ansible_facts["os_family"] == "Archlinux" and
     not (home_hayase_appimage_stat.stat.exists | default(false)))

- name: Select Hayase Linux release assets
  ansible.builtin.set_fact:
    home_hayase_linux_deb_url: >-
      {{
        home_hayase_release.json
        | dict2items
        | map(attribute='value')
        | select('match', '^https://.*/linux-hayase-.*-linux[.]deb$')
        | first
        | default('')
      }}
    home_hayase_linux_appimage_url: >-
      {{
        home_hayase_release.json
        | dict2items
        | map(attribute='value')
        | select('match', '^https://.*/linux-hayase-.*-linux[.]AppImage$')
        | first
        | default('')
      }}
  when: home_hayase_release is not skipped

- name: Assert Hayase Linux asset was found
  ansible.builtin.assert:
    that:
      - >
        (ansible_facts["os_family"] == "Debian" and home_hayase_linux_deb_url | length > 0) or
        (ansible_facts["os_family"] == "Archlinux" and home_hayase_linux_appimage_url | length > 0)
    fail_msg: "No matching Hayase Linux asset found in {{ hayase_api_url }} response."
  when: home_hayase_release is not skipped

- name: Install Hayase from .deb (Debian)
  ansible.builtin.apt:
    deb: "{{ home_hayase_linux_deb_url }}"
    state: present
  when:
    - ansible_facts["os_family"] == "Debian"
    - home_hayase_pkg_check.stdout | default('') != 'install ok installed'

- name: Install Hayase AppImage (Arch)
  ansible.builtin.get_url:
    url: "{{ home_hayase_linux_appimage_url }}"
    dest: "{{ hayase_appimage_dest }}"
    mode: "0755"
  when:
    - ansible_facts["os_family"] == "Archlinux"
    - not (home_hayase_appimage_stat.stat.exists | default(false))

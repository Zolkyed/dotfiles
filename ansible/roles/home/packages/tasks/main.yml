---
- name: Install packages
  ansible.builtin.package:
    name: "{{
      core_packages +
      utility_packages +
      media_packages +
      office_packages +
      system_packages +
      fun_packages
    }}"
    state: present

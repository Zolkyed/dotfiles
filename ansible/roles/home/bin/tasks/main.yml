---
- name: Ensure ~/.local/bin exists
  ansible.builtin.file:
    path: "{{ bin_dest }}"
    state: directory
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    mode: "0755"

- name: Install custom scripts to ~/.local/bin
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ bin_dest }}/{{ item | basename }}"
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    mode: "0755"
  loop: "{{ query('ansible.builtin.fileglob', role_path ~ '/files/*') }}"

- name: Install konsave command aliases
  ansible.builtin.file:
    src: "{{ bin_dest }}/konsavectl"
    dest: "{{ bin_dest }}/{{ item }}"
    state: link
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    force: true
  loop: "{{ bin_konsave_links }}"

- name: Ensure konsavectl keybind directory exists
  ansible.builtin.file:
    path: "{{ bin_konsave_keybinds_dest }}"
    state: directory
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    mode: "0755"

- name: Install konsavectl KDE keybind overrides
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ bin_konsave_keybinds_dest }}/{{ item | basename }}"
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    mode: "0644"
  loop: "{{ query('ansible.builtin.fileglob', role_path ~ '/keybinds/*') }}"

- name: Ensure ~/.config/homectl exists
  ansible.builtin.file:
    path: "{{ user_home }}/.config/homectl"
    state: directory
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    mode: "0755"

- name: Deploy homectl config
  ansible.builtin.template:
    src: homectl-config.yml.j2
    dest: "{{ user_home }}/.config/homectl/config.yml"
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    mode: "0644"
  no_log: true

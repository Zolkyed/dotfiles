---
- name: Browser | Install Arch package from AUR  # noqa var-naming[no-role-prefix]
  ansible.builtin.include_role:
    name: system/aur
    tasks_from: install_packages
  vars:
    aur_install_packages:
      - "{{ browser_arch_package }}"

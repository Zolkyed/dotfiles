---
- name: AppImage | Create Applications directory
  ansible.builtin.file:
    path: "{{ user_home }}/Applications"
    state: directory
    owner: "{{ user_name }}"
    group: "{{ user_name }}"
    mode: "0755"
